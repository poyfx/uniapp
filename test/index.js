const successCode = 0 // 成功的code
const tokenExpireCode = -10000 // token 过期的code (举例)
// const overtimeCode = 303 // 请求超时了的code,模拟


const currentCode = tokenExpireCode // 当前想到测试的状态
export {
  successCode,
  tokenExpireCode,
  // overtimeCode,
  currentCode
}
